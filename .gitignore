# Env
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build output
dist/
coverage/

# TypeScript incremental build state. MUST stay untracked: `composite: true`
# means tsc trusts a tsbuildinfo over the (gitignored, therefore absent) dist/
# on a fresh clone and skips emitting, which breaks the very first build in CI.
*.tsbuildinfo

# Generated registry: emitted into the entry package by build-tools on every
# path that builds, starts, or typechecks. There is no committed stand-in for
# it; generation always precedes compilation.
app/src/generated/microservice-registry.ts

# Generated Dockerfile (emitted by scripts/emit-effective-dockerfile.sh from
# Dockerfile.template; the template is tracked)
Dockerfile

# Editor junk
.idea/
.vscode/
*.swp
*.swo
*~

# OS junk
.DS_Store
Thumbs.db
