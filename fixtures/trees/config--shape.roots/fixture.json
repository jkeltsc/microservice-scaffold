{
  "expectedDiagnostic": "[config:shape]",
  "entryPoint": "runRepoInvariantsCli",
  "fault": "The 'roots' key is declared as a string.",
  "partition": {
    "kind": "tree",
    "justification": "An npm clean install over this tree would fail: it is read as a Project_Directory whose wrong-typed roots is the subject, never installed."
  }
}
