{
  "expectedDiagnostic": "[config:unparsable]",
  "entryPoint": "runRepoInvariantsCli",
  "fault": "scaffold.config.json is not valid JSON.",
  "partition": {
    "kind": "tree",
    "justification": "An npm clean install over this tree would fail: it is read as a Project_Directory whose malformed scaffold.config.json is the subject, never installed."
  }
}
