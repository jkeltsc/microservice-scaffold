{
  "expectedDiagnostic": "[config:shape]",
  "entryPoint": "runRepoInvariantsCli",
  "fault": "The 'entry' key is declared as an object.",
  "partition": {
    "kind": "tree",
    "justification": "An npm clean install over this tree would fail: it is read as a Project_Directory whose wrong-typed entry is the subject, never installed."
  }
}
