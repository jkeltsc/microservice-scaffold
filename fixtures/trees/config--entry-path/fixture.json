{
  "expectedDiagnostic": "[config:entry-path]",
  "entryPoint": "runRepoInvariantsCli",
  "fault": "The declared 'entry' value is not a Valid_Root_Path.",
  "partition": {
    "kind": "tree",
    "justification": "An npm clean install over this tree would fail: it is read as a Project_Directory whose invalid Entry_Root path is the subject, never installed."
  }
}
