{
  "expectedDiagnostic": "[config:root-framework]",
  "entryPoint": "runRepoInvariantsCli",
  "fault": "A Discovery_Root collides with a Framework_Singleton directory.",
  "partition": {
    "kind": "tree",
    "justification": "An npm clean install over this tree would fail: it is read as a Project_Directory whose Discovery_Root colliding with a Framework_Singleton is the subject, never installed."
  }
}
