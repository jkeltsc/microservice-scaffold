// Empty microservice registry template — committed so a fresh clone compiles.
// If the generated file's export name or type import changes, update this to
// match. The generator's template literal in
// packages/build-tools/src/generate-registry.ts is the source of truth.
import type { MicroserviceRegistry } from "@microservices/contracts";
export const microserviceRegistry: MicroserviceRegistry = [];
